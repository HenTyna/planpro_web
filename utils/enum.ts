export enum Path {
    TRIP = "/trips",
    PLAN = "/plans",
    NOTES = "/notes",
    REMINDER = "/reminder",
    TODO = "/todos",
    CHAT = "/chat-ai",
    CALENDAR = "/calendar",
    LOGIN = "/login",
    
}

export enum ConfirmationType {
    DELETE = 'DELETE',
    UPDATE = 'UPDATE',
    CREATE = 'CREATE',
    REMOVE = 'REMOVE',
}